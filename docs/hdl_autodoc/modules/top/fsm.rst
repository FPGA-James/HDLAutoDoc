top — State Machine
===================

No FSM detected in ``top.vhd``.

This module is either a structural wrapper (no combinational
state logic) or uses a coding style not yet supported by the
FSM extractor.
