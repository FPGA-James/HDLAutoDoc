# ── Documentation engine ─────────────────────────────────────────────────────
sphinx>=7.0,<9.0

# Pin docutils — sphinxvhdl 0.2.2 has a transition node bug with docutils 0.21+
docutils>=0.18,<0.21

# ── Theme ─────────────────────────────────────────────────────────────────────
furo>=2025.12.19

# ── Sphinx extensions ─────────────────────────────────────────────────────────
# WaveDrom timing diagrams via .. wavedrom:: directive
sphinxcontrib-wavedrom>=3.0

# VHDL entity auto-documentation domain
sphinx-vhdl>=0.1

# Offline WaveDrom rendering — used during PDF (latexpdf) builds
wavedrom

# ── Register map generation ───────────────────────────────────────────────────
# Generates VHDL packages, AXI-Lite wrappers, C headers, and HTML from
# TOML/YAML register definitions (see scripts/registers/generate.py)
hdl-registers>=8.0

# ── Development / testing ─────────────────────────────────────────────────────
pytest>=8.0
